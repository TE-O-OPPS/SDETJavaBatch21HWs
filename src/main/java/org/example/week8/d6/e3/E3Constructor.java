package org.example.week8.d6.e3;

public class E3Constructor {
    public static void main(String[] args) {

// Hint: In the `StoreProduct` class, declare instance variables for `label`, `price`, `category`, `hasExpiration`, and `stock`.
// Hint: Use the main constructor to initialize all the instance variables directly with the provided values.
// Hint: In the second constructor, set `category` to `"misc"` and `hasExpiration` to `false` by default when not provided.
// Hint: In the third constructor, set `category` to `"misc"`, `hasExpiration` to `false`, and `stock` to `0` when not provided.
// Hint: Use the `display` method to print all instance variables in the format: "label price category hasExpiration stock".
    }
}
