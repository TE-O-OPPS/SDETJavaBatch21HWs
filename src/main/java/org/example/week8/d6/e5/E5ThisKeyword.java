package org.example.week8.d6.e5;

public class E5ThisKeyword {
    public static void main(String[] args) {

        // Hint: In the `Employee` class, declare private instance variables for `name`, `lastName`, `employeeId`, `startDate`, and `salary`.
// Hint: Use the no-argument constructor to initialize all instance variables with default values like `null` and `0`.
// Hint: In the parameterized constructor, assign the values passed as parameters to the instance variables using the `this` keyword.
// Hint: Use the `printDetails` method to print the employee's details in the format: "Name LastName EmployeeId StartDate Salary".
// Hint: In the `Main` class, create one `Employee` object using the no-argument constructor and another using the parameterized constructor, then call `printDetails` for both.

    }
}
