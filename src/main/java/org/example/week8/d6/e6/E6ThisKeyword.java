package org.example.week8.d6.e6;

public class E6ThisKeyword {
    public static void main(String[] args){
        // Hint: In the `ShoppingStore` class, declare private instance variables for `item`, `price`, and `quantity`.
// Hint: Create a constructor to initialize the instance variables using the values provided as parameters.
// Hint: Use the `itemTotalPrice` method to calculate the total price by multiplying `price` by `quantity` and print the result in the format: "Item Total Value TotalValue".
// Hint: Ensure that the `itemTotalPrice` method returns the calculated total price to be used in the `Main` class.
// Hint: In the `Main` class, call `itemTotalPrice` for each object, store the returned values, and add them to calculate the total sum. Print the total sum in the format: "You purchased TotalSum Today".

    }
}
   