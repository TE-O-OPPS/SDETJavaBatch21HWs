package org.example.week8.d5.e5;
public class E5AccessModifiers {

       public static void main(String[] args) {
// Hint: In `AnotherClass`, create a private method that returns "private" but note it cannot be accessed from the main class.
// Hint: Use a default method in `AnotherClass` to return "default" and access it within the same package.
// Hint: Create a protected method in `AnotherClass` to return "protected" and access it from a subclass or the same package.
// Hint: Create a public method in `AnotherClass` to return "public" and access it from anywhere.
// Hint: In the main class, call only the accessible methods (default, protected, public) and print their results.
       }
}
